<%= form_with(model: student_class_relationship, local: true) do |form| %>
  <% if student_class_relationship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(student_class_relationship.errors.count, "error") %> prohibited this student_class_relationship from being saved:</h2>

      <ul>
      <% student_class_relationship.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :student_classes_id %>
    <%= form.text_field :student_classes_id, id: :student_class_relationship_student_classes_id %>
  </div>

  <div class="field">
    <%= form.label :students_id %>
    <%= form.text_field :students_id, id: :student_class_relationship_students_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
