<%= render partial: "layouts/header" %>
<div class='form-style-5'>
  <p id="notice"><%= notice %></p>

  <p>
    <strong>Título:</strong>
    <%= @clinic_case.title %>
  </p>
  
  <p>
    <strong>Descrição do caso:</strong>
    <%= @clinic_case.description %>
  </p>
  <table>
    <tr>
      <% @clinic_case.images.each_with_index do |image, i| %>
        <% if i != 0 && i % 3 == 0 %>
          </tr><tr>
        <%end%>
        <td><%= image_tag(image, :class => "image") %></td>
          <br>
      <%end%>
    </tr>
  </table>
  <p>
    <strong>Informações Clinicas:</strong>
    <%= @clinic_case.clinicInformation %>
  </p>

  <p>
    <strong>Evolução do caso:</strong>
    <%= @clinic_case.caseEvolution %>
  </p>

  <p>
    <strong>Informações extras:</strong>
    <%= @clinic_case.extraInformation %>
  </p>

  <%= link_to 'Edit', edit_clinic_case_path(@clinic_case) %> |
  <%= link_to 'Back', clinic_cases_path %>
</div>