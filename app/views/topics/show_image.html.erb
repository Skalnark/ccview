<%= render partial: "layouts/header" %>
<div class='form-style-5', style="width: fit-content;">

  <div style="text-align:center; width: fit-content;">
    <%= image_tag(@topic.image) if @topic.image.attached? %><br>

    <p>
    <strong><%= @topic.image_label %></strong>
    </p>

    <p>
      <strong>Descrição:</strong>
      <pre><%= @topic.image_description %></pre>
    </p>
  </div>

  <div class="actions">
    <%= link_to image_tag("ico04.png"), case_module_topic_path(@module, @topic) %>
  </div>
</div>